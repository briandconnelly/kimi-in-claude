# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
.env
.env.*

# Secrets / credentials (never commit)
*.pem
*.key
*.p12
*.pfx
credentials.json
*_credentials.json

# Tooling caches
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
.ty_cache/

# OS
.DS_Store

docs/superpowers/

# Stray kimi state directories. A test that points KIMI_CODE_HOME at an unexpandable
# path (a literal "~user") makes the CLI create that directory relative to the CWD.
# Ignored as belt-and-braces; the real fix is that unit tests never spawn the CLI
# (see tests/conftest.py).
~*/
.kimi-code/
